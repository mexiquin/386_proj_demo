#include "example.h"

int calcSum(int num1, int num2)
{
    return num1 + num2;
}

bool isEven(int number)
{
    if(number % 2 == 0)
    {
        return true;
    } else {
        return false;
    }
}